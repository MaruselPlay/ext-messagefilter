name: Build MessageFilter Extension

on:
  push:
    branches: [ main, master, develop ]
  pull_request:
    branches: [ main, master, develop ]
  release:
    types: [ created ]

jobs:
  build-linux:
    name: Build Linux (ZTS) - PHP ${{ matrix.php }}
    runs-on: ubuntu-22.04
    strategy:
      fail-fast: false
      matrix:
        include:
          - php: '8.3'
            patch: '8.3.24'
          - php: '8.4'
            patch: '8.4.14'
          - php: '8.5'
            patch: '8.5.7'
    
    steps:
      - name: Checkout extension code
        uses: actions/checkout@v4
      
      - name: Install dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y \
            build-essential \
            autoconf \
            automake \
            libtool \
            bison \
            re2c \
            pkg-config \
            libxml2-dev \
            libsqlite3-dev \
            libssl-dev \
            libcurl4-openssl-dev \
            libonig-dev \
            libzip-dev \
            libreadline-dev \
            zlib1g-dev
      
      - name: Download PHP ${{ matrix.patch }} source
        run: |
          cd /tmp
          wget https://www.php.net/distributions/php-${{ matrix.patch }}.tar.gz
          tar -xzf php-${{ matrix.patch }}.tar.gz
          
      - name: Compile PHP ${{ matrix.patch }} with ZTS
        run: |
          cd /tmp/php-${{ matrix.patch }}
          ./configure \
            --enable-zts \
            --prefix=/opt/php-zts \
            --with-config-file-path=/opt/php-zts/etc \
            --enable-mbstring \
            --with-curl \
            --with-openssl \
            --with-zlib \
            --disable-cgi
          make -j$(nproc)
          sudo make install
      
      - name: Build MessageFilter extension
        run: |
          /opt/php-zts/bin/phpize
          ./configure --with-php-config=/opt/php-zts/bin/php-config
          make
          make test || true
      
      - name: Verify extension
        run: |
          EXT_DIR=$(/opt/php-zts/bin/php-config --extension-dir)
          sudo cp modules/messagefilter.so $EXT_DIR/
          /opt/php-zts/bin/php -d extension=messagefilter.so -m | grep messagefilter
          /opt/php-zts/bin/php -d extension=messagefilter.so -r "echo 'ZTS: ' . (ZEND_THREAD_SAFE ? 'enabled' : 'disabled') . PHP_EOL;"
      
      - name: Create release package
        run: |
          mkdir -p release
          cp modules/messagefilter.so release/
          cp README.md release/ || touch release/README.md
          cd release
          tar -czf messagefilter-php${{ matrix.php }}-linux-x64-ts.tar.gz messagefilter.so README.md
      
      - name: Upload Linux artifact
        uses: actions/upload-artifact@v4
        with:
          name: messagefilter-php${{ matrix.php }}-linux-x64-ts
          path: release/messagefilter-php${{ matrix.php }}-linux-x64-ts.tar.gz

  build-windows:
    name: Build Windows (ZTS) - PHP ${{ matrix.php }}
    runs-on: windows-2022
    strategy:
      fail-fast: false
      matrix:
        php: ['8.3', '8.4', '8.5']
        arch: [x64]
        ts: [ts]
    
    steps:
      - name: Checkout extension code
        uses: actions/checkout@v4
      
      - name: Build extension
        uses: php/php-windows-builder/extension@v1
        with:
          php-version: ${{ matrix.php }}
          arch: ${{ matrix.arch }}
          ts: ${{ matrix.ts }}
          args: --enable-messagefilter
      
      - name: Package extension
        shell: powershell
        run: |
          $dll = Get-ChildItem -Recurse -Filter "php_messagefilter.dll" | Select-Object -First 1
          
          if ($dll) {
            Write-Host "Found DLL: $($dll.FullName)"
            New-Item -ItemType Directory -Force -Path release
            Copy-Item $dll.FullName release\php_messagefilter.dll
            Compress-Archive -Path release\* -DestinationPath "messagefilter-php${{ matrix.php }}-windows-x64-ts.zip"
          } else {
            Write-Error "DLL not found"
            exit 1
          }
      
      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: messagefilter-php${{ matrix.php }}-windows-x64-ts
          path: messagefilter-php${{ matrix.php }}-windows-x64-ts.zip

  create-release:
    name: Create GitHub Release
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    if: github.event_name == 'release'
    permissions:
      contents: write
    
    steps:
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts
          pattern: messagefilter-*
          merge-multiple: true
      
      - name: Upload to Release
        uses: softprops/action-gh-release@v2
        with:
          files: ./artifacts/*
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  create-development-release:
    name: Create Development Build Release
    needs: [build-linux, build-windows]
    runs-on: ubuntu-latest
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master' || github.ref == 'refs/heads/develop')
    permissions:
      contents: write
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Generate build version and date
        id: build_info
        run: |
          SHORT_SHA=$(git rev-parse --short HEAD)
          BUILD_NUM=$(git rev-list --count HEAD)
          BUILD_VERSION="build-${BUILD_NUM}-${SHORT_SHA}"
          BUILD_DATE=$(date -u '+%Y-%m-%d %H:%M:%S UTC')
          
          echo "version=${BUILD_VERSION}" >> $GITHUB_OUTPUT
          echo "date=${BUILD_DATE}" >> $GITHUB_OUTPUT
      
      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: ./artifacts
          pattern: messagefilter-*
          merge-multiple: true
      
      - name: Generate release notes
        run: |
          cat > release_notes.md << EOF
          ## Latest Development Build
          
          **Commit**: \`${{ github.sha }}\`
          **Branch**: \`${{ github.ref_name }}\`
          **Date**: ${{ steps.build_info.outputs.date }}
          
          ### Latest Changes
          $(git log -1 --pretty=format:'- %s')
          
          ### Files Available
          - \`messagefilter-php8.X-linux-x64-ts.tar.gz\` - Linux x64 (Thread-Safe)
          - \`messagefilter-php8.X-windows-x64-ts.zip\` - Windows x64 (Thread-Safe)
          
          > ⚠️ This is an automated development build. For stable releases, see tagged versions.
          
          This release is automatically updated with every commit to active branches.
          EOF
      
      - name: Delete old development release
        continue-on-error: true
        run: |
          gh release delete dev-latest --yes || true
          git push origin :refs/tags/dev-latest || true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      
      - name: Create Latest Development Build release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: dev-latest
          name: "Latest Development Build"
          body_path: release_notes.md
          files: ./artifacts/*
          draft: false
          prerelease: true
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}